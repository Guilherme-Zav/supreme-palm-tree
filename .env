# Banco de dados local (SQLite) — usado pelo Prisma e pelo Next.
# Este arquivo NÃO contém segredos e pode ser versionado.
# Sua chave da Anthropic vai em .env.local (veja .env.example).
DATABASE_URL="file:./dev.db"
